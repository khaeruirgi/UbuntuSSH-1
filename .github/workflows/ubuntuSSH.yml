name: SSHserver
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        GOOS: [ NFS-Project ]
        GOARCH: [ Step 1 ]
        GOOS: [ NFS-Project ]
        GOARCH: [ Step 2 ]
    steps:
    - uses: actions/checkout@v2
    - name: Session Setup
      uses: mxschmitt/action-tmate@master
