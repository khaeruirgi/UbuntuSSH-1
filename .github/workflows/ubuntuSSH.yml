name: SSHserver
on: 
  workflow_dispatch:
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        GOOS: [ NFS-Project ]
        GOARCH: [ Kernel Build ]
    steps:
    - uses: actions/checkout@v2
    - name: Session Setup
      uses: mxschmitt/action-tmate@master
